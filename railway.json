{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn --bind 0.0.0.0:$PORT --workers 2 --timeout 120 --access-logfile - --error-logfile - backend:app",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 300
  }
}
